# Copy this file, change the necessary settings and remove '.example' from the name.
# You should end up with a '.env' and a '.env.example' file in your Labelit root folder.

# Change admin password ("t0") to any string. You need to login every 14 days.
ADMIN_PASSWORD=t0

# Change to any random string (16 characters). Don't change the this after starting a project! This is used to encrypt texts in the database. 
SECRET=3450482QT340912A


# The following settings only need to be changed if you run Labelit remotely

# No need to change if mongodb is running on the same host as node
MONGODB_URI=mongodb://localhost/labelit

# Usually you don't need to change the port
PORT=8000
